# this is a program to print hello world
print("hello world")

import pandas as pd
import numpy as np

a = 3
b = 4
print(a/b)

git remote get -url origin

git checkout -b hello-world

git add hello-world.py 
git commit --amend

# remove all the updates and go back to last commited work
git checkout hello-world.py 

git stash -m "write some comment"

git stash pro



$ git push --set-upstream origin hello-world

$ git push --set-upstream origin hello-world